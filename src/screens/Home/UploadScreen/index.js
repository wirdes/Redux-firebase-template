import React from 'react';
import {View, Text} from 'react-native';

const Upload = props => {
  return (
    <View style={{flex: 1, alignItems: 'center'}}>
      <Text style={{fontSize: 52}}>Upload Screen</Text>
    </View>
  );
};

export default Upload;
